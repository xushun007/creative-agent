# =============================================================================
# Creative Agent 配置文件示例
# =============================================================================
# 
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填写必需的配置项（标记为 REQUIRED）
# 3. 根据需要调整可选配置项
# 
# 注意：
# - 环境变量名可以带 CTV_ 前缀或不带（不区分大小写）
# - 配置优先级：代码传参 > .env 文件 > 环境变量 > 默认值
# =============================================================================

# =============================================================================
# 模型配置 (Model Configuration)
# =============================================================================

# API密钥 (REQUIRED)
# 从你的 AI 服务提供商获取
OPENAI_API_KEY=sk-your-api-key-here

# 或者使用 CTV_ 前缀
# CTV_API_KEY=sk-your-api-key-here

# 模型提供商 (可选，默认：deepseek)
# 支持：openai, anthropic, deepseek, qwen 等
CTV_MODEL_PROVIDER=deepseek

# 模型名称 (可选，默认：qwen-plus)
# 示例：gpt-4, gpt-3.5-turbo, claude-3-opus, deepseek-chat, qwen-plus 等
CTV_MODEL=qwen-plus

# API基础URL (可选)
# 如果使用自定义端点或代理，请设置此项
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# CTV_API_BASE=https://api.openai.com/v1

# =============================================================================
# 工作目录配置 (Workspace Configuration)
# =============================================================================

# 代码工作目录 (可选，默认：./workspace)
# 所有文件操作都在此目录下进行
CTV_CWD=./workspace

# =============================================================================
# 安全策略配置 (Security Policy)
# =============================================================================

# 命令批准策略 (可选，默认：on_request)
# - always: 总是询问用户批准
# - on_request: 仅在执行敏感操作时询问
# - never: 不询问，直接执行（谨慎使用）
CTV_APPROVAL_POLICY=on_request

# 沙箱策略 (可选，默认：workspace_write)
# - strict: 严格限制，只读工作目录
# - workspace_write: 工作目录可写，其他只读
# - none: 无限制（谨慎使用）
CTV_SANDBOX_POLICY=workspace_write

# =============================================================================
# 系统提示配置 (System Prompt Configuration)
# =============================================================================

# 基础系统指令 (可选)
# CTV_BASE_INSTRUCTIONS=You are Codex, an AI coding assistant.

# 用户自定义指令 (可选)
# 会附加到基础指令后
# CTV_USER_INSTRUCTIONS=Please write code in Python 3.10+ style.

# =============================================================================
# 模型参数配置 (Model Parameters)
# =============================================================================

# 最大输出token数 (可选，默认：4096)
# 范围：1 - 128000
CTV_MAX_TOKENS=4096

# 采样温度 (可选，默认：0.1)
# 范围：0.0 - 2.0
# 值越小，输出越确定；值越大，输出越随机
CTV_TEMPERATURE=0.1

# =============================================================================
# 执行控制配置 (Execution Control)
# =============================================================================

# 单次任务最大对话轮次 (可选，默认：20)
# 范围：1 - 100
CTV_MAX_TURNS=20

# =============================================================================
# 日志配置 (Logging Configuration)
# =============================================================================

# 日志级别 (可选，默认：INFO)
# 可选值：DEBUG, INFO, WARNING, ERROR
CTV_LOG_LEVEL=INFO

# =============================================================================
# 其他配置 (Other Configuration)
# =============================================================================

# 禁用响应存储 (可选，默认：false)
# 设置为 true 则不保存对话历史到文件
CTV_DISABLE_RESPONSE_STORAGE=false

# =============================================================================
# 开发环境专用配置 (Development Only)
# =============================================================================

# 调试模式 (开发用)
# DEBUG=true

# 详细日志 (开发用)
# VERBOSE=true
